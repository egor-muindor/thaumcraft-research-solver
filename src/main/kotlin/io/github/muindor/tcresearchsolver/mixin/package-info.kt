// Placeholder so the GTNH convention can resolve `mixinsPackage` (it requires the
// directory to exist) before real mixins arrive in Phase 4. The ResearchTweaks GUI
// mixin (ResearchTableGuiFactoryMixin) will live in this package.
package io.github.muindor.tcresearchsolver.mixin
